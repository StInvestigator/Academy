<UserControl x:Class="Academy.Presentation.Pages.Student.MainInfo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Academy.Presentation.Pages.Student"
             mc:Ignorable="d" 
             d:DesignHeight="309" d:DesignWidth="800" FontSize="40" FontFamily="Gabriola Bold">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel> <!--Grades-->
            <Label Content="Recent Grades" HorizontalAlignment="Center" Foreground="#fcf5e5"/>
            <ListView>
                
            </ListView>
        </StackPanel>

        <StackPanel  Grid.Column="1"> <!--Schedule-->
            <Label Content="Tomorrow Schedule" HorizontalAlignment="Center" Foreground="#fcf5e5" FontSize="40"/>
            <ListView>

            </ListView>
        </StackPanel>

        <StackPanel  Grid.Column="2"> <!--Tasks-->
            <Label Content="Recent Grades" HorizontalAlignment="Center" Foreground="#fcf5e5"/>
            <ListView>

            </ListView>
        </StackPanel>
    </Grid>
</UserControl>
